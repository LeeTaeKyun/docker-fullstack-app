language: generic

sudo: required

services:
  - docker

before_install:
  - docker build -t meanthe/docker-app -f ./frontend/Dockerfile.dev ./frontend

script: 
  - docker run -e CI=true meanthe/docker-app npm run test

after_success:
  - docker build -t meanthe/docker-frontend ./frontend
  - docker build -t meanthe/docker-backend ./backend
  - docker build -t meanthe/docker-nginx ./nginx

  - echo "$DOCKER_HUB_PASSWORD" | docker login -u "$DOCER_HUB_ID" --password-stdin

  - docker push meanthe/docker-frontend
  - docker push meanthe/docker-backend
  - docker push meanthe/docker-nginx